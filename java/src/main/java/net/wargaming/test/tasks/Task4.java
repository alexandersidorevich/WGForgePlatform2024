package net.wargaming.test.tasks;

import java.io.File;
import java.util.List;

// Task №4
// ------------------------------------------------------------------------------------------------
// Write a function fileList(directory) that searches for text files within the specified directory and recursively bypasses all the subdirectories.
public final class Task4 {

    /**
     *  Recursively search for text files (with .txt extension ) within the specified directory.
     *
     * @param directory The directory to search for text files.
     * @return A sequence of paths to text files found within the directory.
     */
    public static List<File> fileList(String directory) {
        throw new UnsupportedOperationException("Not implemented yet.");
    }
}